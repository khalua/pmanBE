<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for(:title) || "Prompt" %></title>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="h-full bg-gray-50">
  <nav class="bg-white shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex items-center">
          <%= link_to "Prompt", root_path, class: "text-xl font-bold text-indigo-600" %>
        </div>
        <div class="flex items-center space-x-4">
          <% if current_user %>
            <span class="text-gray-700 text-sm"><%= current_user.name %></span>
            <%= button_to "Log out", logout_path, method: :delete, class: "text-sm text-gray-500 hover:text-gray-700" %>
          <% else %>
            <%= link_to "Log in", login_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
            <%= link_to "Register", register_path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700" %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>

  <% if flash[:notice] %>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
      <div class="rounded-md bg-green-50 p-4">
        <p class="text-sm text-green-800"><%= flash[:notice] %></p>
      </div>
    </div>
  <% end %>

  <% if flash[:alert] %>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
      <div class="rounded-md bg-red-50 p-4">
        <p class="text-sm text-red-800"><%= flash[:alert] %></p>
      </div>
    </div>
  <% end %>

  <main>
    <%= yield %>
  </main>

  <footer class="bg-white border-t mt-auto">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <div class="flex justify-between items-center">
        <p class="text-gray-400 text-sm">&copy; <%= Date.current.year %> Prompt. All rights reserved.</p>
        <div class="space-x-4">
          <%= link_to "Privacy", privacy_path, class: "text-sm text-gray-400 hover:text-gray-600" %>
          <%= link_to "Terms", terms_path, class: "text-sm text-gray-400 hover:text-gray-600" %>
        </div>
      </div>
    </div>
  </footer>
</body>
</html>
