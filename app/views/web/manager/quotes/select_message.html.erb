<% content_for(:title) { "Send Vendor Notification - Prompt" } %>

<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h1 class="text-2xl font-bold text-gray-900 mb-2">Quote Approved</h1>
  <p class="text-sm text-gray-600 mb-6">
    The quote from <strong><%= @quote.vendor.name %></strong> has been approved.
    How would you like the vendor to proceed?
  </p>

  <div class="bg-white shadow rounded-lg p-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4">Choose notification message:</h2>

    <div class="space-y-4">
      <%= button_to send_approval_message_web_manager_quote_path(@quote, message_type: "contact_tenant"),
          method: :post,
          class: "w-full text-left p-6 border-2 border-gray-300 rounded-lg hover:border-blue-500 hover:bg-blue-50 transition cursor-pointer" do %>
        <div class="font-semibold text-gray-900 mb-2">
          📞 Vendor should contact tenant directly
        </div>
        <div class="text-sm text-gray-600">
          Sends tenant's name, phone, and email to the vendor so they can coordinate directly.
        </div>
      <% end %>

      <%= button_to send_approval_message_web_manager_quote_path(@quote, message_type: "manager_will_contact"),
          method: :post,
          class: "w-full text-left p-6 border-2 border-gray-300 rounded-lg hover:border-blue-500 hover:bg-blue-50 transition cursor-pointer" do %>
        <div class="font-semibold text-gray-900 mb-2">
          💬 I will contact the vendor shortly
        </div>
        <div class="text-sm text-gray-600">
          Notifies vendor that you'll reach out with more details and next steps.
        </div>
      <% end %>
    </div>
  </div>

  <div class="mt-6">
    <%= link_to "← Back to Maintenance Request",
        web_manager_maintenance_request_path(@quote.maintenance_request),
        class: "text-sm text-blue-600 hover:text-blue-800" %>
  </div>
</div>
