<% content_for(:title) { "New Vendor - Prompt" } %>

<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Add New Vendor</h1>
    <%= link_to "Back to Vendors", web_manager_vendors_path, class: "text-sm text-blue-600 hover:text-blue-800" %>
  </div>

  <%= render "form", vendor: @vendor %>
</div>
